-module(logallow).
-compile(export_all).

log_modules() -> [
%    wf_core,
    n2o_bullet,
    routes,
%    okey_bot,
    game_session,
%    bullet_handler,
    okey_table,
    okey_desk,
    okey_scoring,
    journal,
%    lucky,
%    n2o_secret,
    js_session,
    okey
].
